version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd news-project-frontend
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: news-project-frontend/build
    files:
      - '**/*'
  cache:
    paths:
      - news-project-frontend/node_modules/**/*